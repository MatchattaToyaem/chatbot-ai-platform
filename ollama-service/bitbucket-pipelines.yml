image: atlassian/default-image:4

definitions:
  steps:
    - step: &build-push
        name: Build and Push to ACR
        services:
          - docker
        script:
          - echo $ACR_PASSWORD | docker login oconnoraiplatform.azurecr.io
              --username $ACR_USERNAME
              --password-stdin
          - docker build -t oconnoraiplatform.azurecr.io/ollama-service:$BITBUCKET_COMMIT .
          - docker tag oconnoraiplatform.azurecr.io/ollama-service:$BITBUCKET_COMMIT
              oconnoraiplatform.azurecr.io/ollama-service:latest
          - docker push oconnoraiplatform.azurecr.io/ollama-service:$BITBUCKET_COMMIT
          - docker push oconnoraiplatform.azurecr.io/ollama-service:latest

    - step: &deploy
        name: Deploy to Azure Container Apps
        script:
          - curl -sL https://aka.ms/InstallAzureCLIDeb | bash
          - az login --service-principal
              --username $AZURE_CLIENT_ID
              --password $AZURE_CLIENT_SECRET
              --tenant $AZURE_TENANT_ID

          - sed -i "s/PLACEHOLDER_ACR/$ACR_PASSWORD/g" ollama.yaml
          - sed -i "s|oconnoraiplatform.azurecr.io/ollama-service:latest|oconnoraiplatform.azurecr.io/ollama-service:$BITBUCKET_COMMIT|g" ollama.yaml
          - |
            if az containerapp show --name ollama-service --resource-group ai-platform 2>/dev/null; then
              az containerapp update \
                --name ollama-service \
                --resource-group ai-platform \
                --yaml ollama.yaml
            else
              az containerapp create \
                --name ollama-service \
                --resource-group ai-platform \
                --yaml ollama.yaml
            fi

pipelines:
  default:
    - step: *build-push

  branches:
    main:
      - step: *build-push
      - step: *deploy
